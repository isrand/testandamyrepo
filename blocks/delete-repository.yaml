name: Delete repository
description: Delete a repository, do a foo, get a bit of bar, and lorem ipsum dolor sit amet all the way.
fullDescription: >
  N/A
type: Block

provider: GitHub
inputs:
  - name: Repository name
    type: string
    description: ""
    key: repositoryName

outputs: []
actions:
  - type: "github:repo:delete"
    fields:
      - name: Owner
        key: owner
        type: text
        value: isrand
      - name: Repository
        key: repo
        type: text
        value: repositoryName

